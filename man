man 1 "22 August 2021" "simple\_shell man page"

## NAME
*simple\_shell*
Program that emulate shell

## SYNOPSIS

**\#include "shell\.h"**

*int main\(int argc \_\_attribute\_\_\(\(unused\)\), char \*\*argv, char \*\*environment\)*

## DESCRIPTION

The simple\_shell program executes a main program that
creates and displays a prompt,
reads a n number of arguments, tokenizes the args
received, if the function is built in the program executes,
and if the function is Not Built in, finds in the path,
reads the function in the directory and executes to finally
return to the prompt

## FUNCTION BUILT IN

* env

* exit

## INVOCATION

This program doesnt need a previous instalation but this needs
compilates the program with this option

_gcc \-Wall \-Werror \-Wextra \-pedantic \-std=gnu89 \*\.c \-o simple\_shell_

Then, you can executes the \./simple\_shell to run your program

## RETURN VALUE

The Return depends de args write in the input of the
program, is not the same if we write a "pwd" command
that if we writes a word that is not a command

## EXAMPLE

**input**

\~/simple\_shell$ \./a\.out
$ pwd

**output**

/home/afecego/simple\_shell
$

**input**

\~/simple\_shell$ \./a\.out
$ 111

**output**

command not found
$


## FILES

* \_path\.c
* \_strcmp\-\_strcat\.c
* \_strdup\.c
* \_strlen\-\_puts\.c
* AUTHORS
* generate\-authors\.sh
* get\_struc\.c
* main\.c
* make\_env\_free\_env\.c
* man
* pathexe\_checkdir\.c
* README\.md
* shell\.h
* tokenizer\.c

## FUNCTIONS INCLUDED

* void path\_\(vars\_f \*vars\)
* void print\_error\(vars\_f \*vars, char \*msg\)
* int \_strcmp\(char \*s1, char \*s2\)
* char \*\_strcat\(char \*s1, char \*s2)
* char \*\_strdup\(char \*s1\)
* unsigned int \_strlen\(char \*str\)
* void \_puts\(char \*str\)
* char \(*get\_struc\(vars\_f \*vars\)\) \(vars\_f \*vars\)
* char func\_exit\(vars\_f \*vars\)
* char func\_env\(vars\_f \*vars\)
* char \*\*make\_env\(char \*\*env2\)
* void free_env\(char \*\*env\)
* void path\_exec\_dir\(vars\_f \*vars\)
* int check_for\_dir\(char \*str\)
* char \*f\_paht\(char \*\*env\)
* char \*\*tokenizer\(char \*buffer, char \*delimiter\)
* char \*\*NewRealloc\(char \*\*cat, size\_t \*size\)
* void path\_concat\(vars\_f \*vars, char \*str2\)

## SEE ALSO

All the details of this project are included in the README file of the project repository in the next link:

<https://github.com/afecego/simple_shell/blob/main/README.md>

## AUTHORS

Adrian Felipe Ceron <https://github.com/afecego>

Paula Carvajal <https://github.com/paulacarvani>